package lstmrg

func ExampleMerge_one() {

	lists := [3][2][]int{

		{{1, 2, 3, 4}, {1, 2, 3, 4}},
		{{}, {}},
		{{}, {0}},
	}

	for _, v := range lists {
		mergedList := mergeTwoLists(NewList(v[0]), NewList(v[1]))
		PrintList(mergedList)
	}
}
